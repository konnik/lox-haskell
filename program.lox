var count = 15;
var previous = 0;
var current = 1;

for (var i=1; i <= count; i = i + 1) {
  print "Fibonacci number " + show i + " is " + show current + ".";

  // Calculate the next Fibonacci number
  var next = previous + current;
  previous = current;
  current = next;
}
